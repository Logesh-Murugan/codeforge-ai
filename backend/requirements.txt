fastapi>=0.100.0
uvicorn[standard]>=0.23.2
sqlalchemy>=2.0.20
asyncpg>=0.28.0
alembic>=1.12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
python-dotenv>=1.0.0
openai==1.54.4
httpx==0.27.2
langgraph>=0.2.0
langchain-core>=0.3.0
psycopg2-binary==2.9.10
email-validator==2.2.0
bcrypt==4.0.1

# ── Memory subsystem ──────────────────────────────────────────────────────────
# Vector store (local ChromaDB — no cloud dependency)
chromadb>=0.5.0

# Note: the following are OPTIONAL — only install what your deployment needs.
# For Ollama embeddings: no extra package needed (httpx already present above).
# For HuggingFace Inference API embeddings: no extra package needed (httpx).
# For local sentence-transformers (offline HF, GPU/CPU):
#   sentence-transformers>=2.7.0
#   torch>=2.0.0
